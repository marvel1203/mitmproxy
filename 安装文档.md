# mitmproxy 修改版安装指南

本文档介绍如何在已安装官方版 mitmproxy 的情况下，安装和使用自己修改后的 mitmproxy 源码版本。指南适用于 macOS 和 CentOS 7 环境。

## 方法概述

有三种主要方式可以使用自己修改的 mitmproxy：

1. **开发模式安装** - 适合经常修改代码并测试的场景
2. **构建安装包** - 适合发布到多台机器的场景
3. **Docker 容器** - 适合在隔离环境中使用的场景

## 1. 开发模式安装 (推荐用于开发阶段)

这种方法会创建一个指向你的源码目录的符号链接，这样你修改代码后无需重新安装即可生效。

### macOS 上的开发模式安装

```bash
# 进入 mitmproxy 源码目录
cd /Users/hb03812/github/mitmproxy

# 使用 pip 安装开发模式
pip install -e .

# 检查安装是否成功
which mitmproxy
mitmproxy --version
```

### CentOS 7 上的开发模式安装

```bash
# 安装必要的依赖
sudo yum install -y python3-devel libffi-devel openssl-devel

# 进入 mitmproxy 源码目录
cd /path/to/your/mitmproxy

# 使用 pip 安装开发模式
pip3 install -e .

# 检查安装是否成功
which mitmproxy
mitmproxy --version
```

## 2. 构建并安装包

如果你想创建一个可以分发的包，你可以构建 wheel 文件。

### macOS 和 CentOS 7 通用步骤

```bash
# 进入 mitmproxy 源码目录
cd /Users/hb03812/github/mitmproxy  # 在 macOS 上
# cd /path/to/your/mitmproxy        # 在 CentOS 7 上

# 安装构建工具
pip install build

# 构建 wheel 包
python -m build --wheel

# 安装构建的包（替换版本号）
pip install dist/mitmproxy-*.whl --force-reinstall
```

## 3. 使用 Docker 容器

如果你需要在隔离环境中使用修改后的 mitmproxy，可以构建 Docker 镜像。

### 创建 Dockerfile

在 mitmproxy 源码目录下创建一个 `Dockerfile`：

```bash
cat > Dockerfile << 'EOF'
FROM python:3.11-slim

WORKDIR /app
COPY . /app/

# 安装依赖和 mitmproxy
RUN pip install --no-cache-dir -e .

# 暴露端口
EXPOSE 8080 8081

# 设置入口点
ENTRYPOINT ["mitmproxy"]
EOF
```

### 构建和运行 Docker 镜像

```bash
# 构建 Docker 镜像
docker build -t custom-mitmproxy .

# 运行 Docker 容器
docker run -p 8080:8080 custom-mitmproxy
```

## 验证安装成功

无论使用哪种方法，你都可以通过以下方式验证你的自定义 mitmproxy 是否成功安装：

```bash
# 验证二进制文件是否来自你的修改版本
which mitmproxy
mitmproxy --version

# 设置代理并测试
export http_proxy="http://localhost:8080"
export https_proxy="http://localhost:8080"
curl -k https://httpbin.org/ip
```

## 如果同时存在官方版本与自定义版本

如果你的系统上已安装了官方版本，并且你想临时使用自己的修改版本，可以：

### 方法1: 使用 Python 虚拟环境

```bash
# 创建虚拟环境
python -m venv mitmproxy-custom
source mitmproxy-custom/bin/activate  # macOS 和 Linux
# 或 mitmproxy-custom\Scripts\activate.bat  # Windows

# 安装你的修改版本
cd /Users/hb03812/github/mitmproxy
pip install -e .

# 使用完后退出虚拟环境
deactivate
```

### 方法2: 直接运行源码目录中的脚本

```bash
# 从源码目录直接运行，不影响全局安装
cd /Users/hb03812/github/mitmproxy
python -m mitmproxy.tools.main
# 或者使用特定工具
python -m mitmproxy.tools.console
python -m mitmproxy.tools.dump
```

## 特别注意事项

### CentOS 7 特殊问题

CentOS 7 自带的 Python 版本较旧，建议使用 SCL 仓库安装新版 Python：

```bash
# 在 CentOS 7 上安装新版 Python
sudo yum install -y centos-release-scl
sudo yum install -y rh-python38
scl enable rh-python38 bash
# 然后再进行上述安装步骤
```

### 解决依赖冲突

如果遇到依赖冲突问题，可以考虑使用 `pipenv` 或 `conda` 创建独立环境。

### 保留原始配置

在安装自定义版本前，备份你的 mitmproxy 配置文件：

```bash
# macOS 上
cp -r ~/.mitmproxy ~/.mitmproxy.backup

# CentOS 7 上
cp -r ~/.mitmproxy ~/.mitmproxy.backup
```

## 常见问题排查

### 问题1: 安装后找不到命令

如果安装后执行 `which mitmproxy` 没有找到命令，可能是 Python 的 bin 目录没有在 PATH 中。解决方法：

```bash
# 查找安装位置
pip show mitmproxy | grep Location
# 添加到PATH（临时）
export PATH=$(pip show mitmproxy | grep Location | cut -d' ' -f2)/bin:$PATH
```

### 问题2: 依赖冲突

如果遇到依赖冲突，尝试：

```bash
pip install -e . --ignore-installed
```

### 问题3: 权限问题

特别是在 CentOS 上，可能遇到权限问题：

```bash
# 使用用户安装模式
pip install --user -e .
```
